import { raw } from "./raw";

// TODO: Implement these colors, replace this placeholder
export const semantic = {
  color: {
    text: {
      primary: raw.gray900,
      muted: raw.gray100,
    },
    action: {
      primary: raw.blue500,
    },
    background: {
      default: raw.gray100,
    },
  },
};